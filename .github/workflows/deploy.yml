name: Deploy to Vercel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install Vercel CLI
      run: npm i -g vercel

    - name: Deploy to Vercel
      run: vercel --token ${{ O7EC4r2Fv9fsT0zBSBY1Nz0c }} --prod # Se der erro, pode tentar colocar --yes
      env:
        VERCEL_ORG_ID: ${{ team_88hy5Num7QhkWWpCzWLi8yLN }}
        VERCEL_PROJECT_ID: ${{ prj_wq4KKsBIhdgtz91MwUvgPJcLvo2e }}
